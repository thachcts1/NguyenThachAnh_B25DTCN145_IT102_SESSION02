#include <stdio.h>
#include <math.h>
	int main(){
		
		int a = 1, b = 1, c = 1;
			int A = pow(a,3) + pow(b,2) + 2 * c + sqrt(a + b - c);
			
			printf("gia tri cua A: %d", A);
			
		
		return 0;
	}
